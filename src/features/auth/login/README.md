# auth / login

Module package — all code for this feature lives here.

- `pages/` — full screen components
- `components/` — module UI parts
- `hooks/` — data + mutations (via mock-api)
- `schemas/` — Zod validation
- `types/` — TypeScript types

See `docs/07-SCREEN-INVENTORY.md` for routes.
