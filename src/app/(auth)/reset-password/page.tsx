import { ResetPasswordPage } from '@/features/auth/reset-password';

export const metadata = { title: 'Reset password', description: 'Choose a new password' };

export default function Page() {
  return <ResetPasswordPage />;
}
