import { CaptivePage } from '@/features/marketing/captive';

export const metadata = { title: 'Captive portal', description: 'Expired session paywall' };

export default function Page() {
  return <CaptivePage />;
}
