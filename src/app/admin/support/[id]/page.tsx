import { SupportTicketDetailPage } from '@/features/admin/support';

export const metadata = { title: 'Ticket Detail', description: 'Support ticket conversation' };

export default function AdminSupportDetailRoute() {
  return <SupportTicketDetailPage />;
}
