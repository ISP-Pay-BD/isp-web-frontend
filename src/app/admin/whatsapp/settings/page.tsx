import { WhatsAppSettingsPage } from '@/features/admin/whatsapp';

export const metadata = { title: 'WhatsApp Settings', description: 'Configure Meta Cloud API and WAHA' };

export default function AdminWhatsAppSettingsRoute() {
  return <WhatsAppSettingsPage />;
}
