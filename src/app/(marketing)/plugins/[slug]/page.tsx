import { PluginDetailPage } from '@/features/marketing/plugins';

export const metadata = { title: 'Plugin', description: 'Plugin detail' };

export default function Page() {
  return <PluginDetailPage />;
}
