import { PaymentReconcilePage } from '@/features/admin/isp-ops';

export const metadata = { title: 'Reconcile', description: 'Payment reconcile log' };

export default function Page() {
  return <PaymentReconcilePage />;
}
