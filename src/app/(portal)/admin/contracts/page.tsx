import { ContractsPage } from '@/features/admin/isp-ops';

export const metadata = { title: 'Contracts', description: 'Contract e-sign' };

export default function Page() {
  return <ContractsPage />;
}
