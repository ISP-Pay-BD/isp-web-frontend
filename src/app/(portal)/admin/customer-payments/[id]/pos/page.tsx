import { PosReceiptPage } from '@/features/admin/isp-ops';

export const metadata = { title: 'POS receipt', description: 'Thermal POS receipt' };

export default function Page() {
  return <PosReceiptPage />;
}
