import { BrandingPage } from '@/features/admin/isp-ops';

export const metadata = { title: 'Branding', description: 'White-label branding' };

export default function Page() {
  return <BrandingPage />;
}
