import { TaxSettingsPage } from '@/features/admin/isp-ops';

export const metadata = { title: 'Tax', description: 'VAT tax settings' };

export default function Page() {
  return <TaxSettingsPage />;
}
