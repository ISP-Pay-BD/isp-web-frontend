import { SidebarPinsPage } from '@/features/admin/sidebar-pins';

export const metadata = { title: 'Sidebar Pins', description: 'Pinned sidebar shortcuts' };

export default function AdminSidebarPinsRoute() {
  return <SidebarPinsPage />;
}
