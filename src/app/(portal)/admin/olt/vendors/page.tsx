import { OltVendorsPage } from '@/features/admin/isp-ops';

export const metadata = { title: 'OLT vendors', description: 'Multi-vendor OLT' };

export default function Page() {
  return <OltVendorsPage />;
}
