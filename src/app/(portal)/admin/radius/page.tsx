import { RadiusPanelPage } from '@/features/admin/isp-ops';

export const metadata = { title: 'RADIUS', description: 'RADIUS CoA PoD panel' };

export default function Page() {
  return <RadiusPanelPage />;
}
