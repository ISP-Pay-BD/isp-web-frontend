import { ReferralAnalyticsPage } from '@/features/admin/isp-ops';

export const metadata = { title: 'Referral analytics', description: 'Referral analytics' };

export default function Page() {
  return <ReferralAnalyticsPage />;
}
