import { RewardsPage } from '@/features/admin/rewards';

export const metadata = { title: 'Rewards', description: 'Referral and reward program management' };

export default function AdminRewardsRoute() {
  return <RewardsPage />;
}
