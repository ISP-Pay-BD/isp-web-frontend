import { RouterDetailPage } from '@/features/admin/isp-ops';

export const metadata = { title: 'Router health', description: 'Router detail health KPIs' };

export default function Page() {
  return <RouterDetailPage />;
}
