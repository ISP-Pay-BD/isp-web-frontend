import { RouterUsersPage } from '@/features/admin/isp-ops';

export const metadata = { title: 'Router users', description: 'Live PPPoE sessions' };

export default function Page() {
  return <RouterUsersPage />;
}
