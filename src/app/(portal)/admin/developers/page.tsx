import { DevelopersPage } from '@/features/admin/isp-ops';

export const metadata = { title: 'Developers', description: 'API keys webhooks' };

export default function Page() {
  return <DevelopersPage />;
}
