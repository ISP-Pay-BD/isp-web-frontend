import { CashbookPage } from '@/features/admin/isp-ops';

export const metadata = { title: 'Cashbook', description: 'Collector cash book' };

export default function Page() {
  return <CashbookPage />;
}
