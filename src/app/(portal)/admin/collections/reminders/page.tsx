import { RemindersPage } from '@/features/admin/isp-ops';

export const metadata = { title: 'Reminders', description: 'Due reminders' };

export default function Page() {
  return <RemindersPage />;
}
