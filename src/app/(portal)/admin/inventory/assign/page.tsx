import { CpeAssignPage } from '@/features/admin/isp-ops';

export const metadata = { title: 'CPE assign', description: 'CPE assignment' };

export default function Page() {
  return <CpeAssignPage />;
}
