import { StockTransfersPage } from '@/features/admin/isp-ops';

export const metadata = { title: 'Transfers', description: 'Stock transfers' };

export default function Page() {
  return <StockTransfersPage />;
}
