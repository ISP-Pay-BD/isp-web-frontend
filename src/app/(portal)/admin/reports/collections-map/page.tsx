import { CollectionsMapPage } from '@/features/admin/isp-ops';

export const metadata = { title: 'Collections map', description: 'Collections route map' };

export default function Page() {
  return <CollectionsMapPage />;
}
