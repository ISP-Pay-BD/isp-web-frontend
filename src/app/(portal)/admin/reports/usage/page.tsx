import { UsageReportPage } from '@/features/admin/isp-ops';

export const metadata = { title: 'Usage', description: 'Bandwidth usage report' };

export default function Page() {
  return <UsageReportPage />;
}
