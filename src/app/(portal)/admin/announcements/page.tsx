import { AnnouncementsPage } from '@/features/admin/isp-ops';

export const metadata = { title: 'Announcements', description: 'Announcement banners' };

export default function Page() {
  return <AnnouncementsPage />;
}
