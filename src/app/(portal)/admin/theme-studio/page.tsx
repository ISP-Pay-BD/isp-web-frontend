import { ThemeStudioPage } from '@/features/admin/theme-studio';

export const metadata = { title: 'Theme Studio', description: 'Customize portal branding and colors' };

export default function AdminThemeStudioRoute() {
  return <ThemeStudioPage />;
}
