import { WhatsAppInboxPage } from '@/features/admin/whatsapp';

export const metadata = { title: 'WhatsApp Inbox', description: 'Unified WhatsApp Business inbox' };

export default function AdminWhatsAppInboxRoute() {
  return <WhatsAppInboxPage />;
}
