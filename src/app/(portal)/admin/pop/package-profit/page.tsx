import { PackageProfitPage } from '@/features/admin/isp-ops';

export const metadata = { title: 'Package profit', description: 'Package profit by POP' };

export default function Page() {
  return <PackageProfitPage />;
}
