import { InactiveCustomersPage } from '@/features/admin/isp-ops';

export const metadata = { title: 'Inactive customers', description: 'Inactive customer list' };

export default function Page() {
  return <InactiveCustomersPage />;
}
