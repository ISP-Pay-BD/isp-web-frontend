import { CustomerGroupsPage } from '@/features/admin/isp-ops';

export const metadata = { title: 'Groups', description: 'Customer groups' };

export default function Page() {
  return <CustomerGroupsPage />;
}
