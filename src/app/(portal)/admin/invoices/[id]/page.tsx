import { InvoiceDetailPage } from '@/features/admin/isp-ops';

export const metadata = { title: 'Invoice', description: 'Invoice detail PDF' };

export default function Page() {
  return <InvoiceDetailPage />;
}
