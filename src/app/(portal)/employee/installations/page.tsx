import { EmployeeInstallPage } from '@/features/admin/engines';

export const metadata = { title: 'My Installations', description: 'Field install jobs' };

export default function Page() {
  return <EmployeeInstallPage />;
}
