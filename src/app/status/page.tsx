import { StatusPage } from '@/features/marketing/status';

export const metadata = { title: 'Status', description: 'Public status page' };

export default function Page() {
  return <StatusPage />;
}
